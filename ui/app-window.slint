import { GroupBox, LineEdit, CheckBox, Button, Slider, VerticalBox } from "std-widgets.slint";

export component AppWindow inherits Window {

    //Main app window
    width: 480px;
    height: 480px;
    title: @tr("Password");
    icon: @image-url("./icon.png");
    background: black;

    //ToDo list
    Layout := VerticalBox {
        // spacing: 0px;
        // padding: 0px;
            Text {
                text: "Password generator";
                color: whitesmoke;
                font-size: 20pt;
                font-family: "Arial";
                horizontal-alignment: center;
            }
        GroupBox {
            LineEdit {
                placeholder-text: "Password";
                horizontal-alignment: center;
            }
            Button {
                text: "Copy";
                primary: true;
            }
        }
        GroupBox {
            Text {

                text: "Length";
            }
            CheckBox {

            }
        }
        GroupBox {
            Text {
                text: "Uppercase";
            }
            CheckBox {

            }
        }
        GroupBox {
            Text {
                text: "Lowercase";
            }
            CheckBox {

            }
        }
        GroupBox {
            Text {
                text: "Numbers";
            }
            CheckBox {

            }
        }
        GroupBox {
            Text {
                text: "Symbols";
            }
            CheckBox {

            }
        }
        Button {
            text: "Generate";
            primary: true;
        }
    }
}